# StockEdu Requirements - Requires Python 3.9+
# Core dependencies
requests>=2.31.0
beautifulsoup4>=4.12.2
lxml>=4.9.3
yfinance>=0.2.18

# Multi-source data providers and async support
aiohttp>=3.8.0
asyncio-throttle>=1.0.2

# News and sentiment analysis
textblob>=0.17.1
vaderSentiment>=3.3.2
newspaper3k>=0.2.8
feedparser>=6.0.10

# Advanced analysis and data processing
numpy>=1.21.0
pandas>=1.3.0
scikit-learn>=1.0.0
matplotlib>=3.5.0
seaborn>=0.11.0

# Performance optimization and monitoring
psutil>=5.9.0
pillow>=9.0.0

# Data integrity and compression
# (Built-in modules used: gzip, sqlite3, pickle, hashlib)

# Error handling and logging
# (Built-in modules used: logging, traceback, threading)

# GUI Requirements
# Note: tkinter is usually included with Python 3.9+, but may need separate installation:
# Ubuntu/Debian: sudo apt-get install python3-tk
# CentOS/RHEL: sudo dnf install python3-tkinter  
# Windows: Usually included with Python installation
# macOS: Usually included with Python installation

# Optional: Alternative GUI framework
# PyQt5>=5.15.9  # Uncomment if you want to use PyQt instead of tkinter

# Development and testing (optional)
# pytest>=7.0.0
# pytest-asyncio>=0.21.0
# pytest-cov>=4.0.0